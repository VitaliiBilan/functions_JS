Умовні конструкції:

1. Написати код, який буде:
Запитати в користувача його вік:
Якщо вік більший 7 і менший 17
	Запитати в користувача ім’я
		Якщо ввів:
			Вивести “Доброго дня, <name>”
		Інакше вивести “До побачення”
Інакше вивести “Ви народились не в 2000х”

2. Без запуску коду написати, що виведеться і обгрунтувати
alert(null || 2 || undefined);
alert(alert(213) || 2 || alert(324));
alert(13123 && null && 2);
alert(alert(1) && alert(2));
alert(null || 2 && 2 || 4);

if(-1 || 0) alert("first");
if (-1 && 0) alert("second");


3. Без запуску коду, написати і обгрунтувати результат
"" + 123 + 0
"" - 143 + 0
true + false
6 / "3"
//"4pt" - 4pt
5 && 213
5 || 0
null + 123
undefined + 10


4. Без запуску, що виведеться?
var name1 = "Andrii"
function myName(name1) {
    alert (name1);
}
myName();
myName("Ivan");


5. Напишіть функцію min (a, b), яка повертає більше чисел a, b.
min(1,2)//2
min(23, 3)//23

Напишіть рекурсивну функцію pow (x, n), яка повертає x в ступені n. Інакше кажучи, примножує x на себе n разів і повертає результат.
pow(2,2); 2*2 = 4;
pow(2, 5); 2*2*2*2*2 = 32;


П.С. У завданнях без запуску, пишіть відповідь в цьому ж рядку як коментар.
ДЗ потрібно запушити у власний репозиторій та надіслати посилання
